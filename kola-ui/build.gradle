description = 'Kola UI module simplifies the use of Anko components thanks to its easy to use UiComponent.'

dependencies {
    implementation project(':kola-core')

    compileOnly "com.android.support:support-fragment:$vSupport"
    compileOnly "org.jetbrains.anko:anko-sdk25:$vAnko"
}